export const name:string = 'Jonnathan Andres Rodriguezzz';
export const age:number = 35;
export let isValid:boolean = false;

export const template:string = `${name}, edad: ${age} `
console.log(template)
